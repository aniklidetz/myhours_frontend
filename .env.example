# MyHours App Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# 🚨 CRITICAL SECURITY: PRODUCTION API CONFIGURATION 🚨
# =============================================================================
# Your production API URL - MUST use HTTPS for security
# The app will REFUSE to start in production mode without a valid HTTPS URL
# Example: EXPO_PUBLIC_API_URL=https://api.yourdomain.com
EXPO_PUBLIC_API_URL=https://your-production-api-domain.com

# =============================================================================
# DEVELOPMENT API CONFIGURATION (OPTIONAL)
# =============================================================================
# Override development API URL (optional)
# Leave blank to use automatic detection (localhost for iOS/web, 10.0.2.2 for Android)
# Example for custom development server:
# EXPO_PUBLIC_DEV_API_URL=http://192.168.1.100:8000
EXPO_PUBLIC_DEV_API_URL=

# Optional: Enable debug logs in development
EXPO_PUBLIC_ENABLE_DEBUG_LOGS=true

# Optional: Enable mock data (for development without backend)
EXPO_PUBLIC_ENABLE_MOCK_DATA=false

# =============================================================================
# 🛡️ SECURITY REQUIREMENTS
# =============================================================================
# 1. NEVER commit your real .env file to version control
# 2. Production API URL MUST use HTTPS (HTTP will be rejected)
# 3. Keep your environment files secure and don't share them
# 4. Use different API endpoints for development, staging, and production
# 5. Ensure your API server has a valid SSL certificate

# =============================================================================
# 📋 PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# Before going live, verify:
# ✅ EXPO_PUBLIC_API_URL is set to your HTTPS production API
# ✅ Your API server has a valid SSL certificate (not self-signed)
# ✅ API server allows CORS from your app domain/mobile app
# ✅ All authentication tokens are stored securely (expo-secure-store)
# ✅ Environment variables are properly configured in your deployment platform
# ✅ Test the app in production mode before releasing